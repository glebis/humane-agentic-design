#!/usr/bin/env python3
"""Entry shim so `scripts/specimen <cmd>` works without PYTHONPATH gymnastics."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))

from tspec.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
